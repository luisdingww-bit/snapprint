# ============================================================
# SnapPrint · AI 模式依赖（需要 NVIDIA GPU + CUDA）
# 离线浮雕模式不需要本文件里的任何东西。
# ============================================================

# 1) PyTorch（必须带 CUDA，按你的显卡驱动版本选择对应的 cuXXX 链接）
#    例如 CUDA 12.1：
#    pip install torch --index-url https://download.pytorch.org/whl/cu121
#    其他版本见 https://pytorch.org/get-started/locally/

# 2) Hunyuan3D-2（图生 3D 形状，质量高）
hy3dgen

# 3) TripoSR（轻量快速，非 PyPI 包，需手动克隆到 models/TripoSR）
#    git clone https://github.com/stabilityai/TripoSR models/TripoSR
#    然后按 models/TripoSR/requirements.txt 安装其依赖
